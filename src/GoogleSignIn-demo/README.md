# GoogleSignIn-demo

this directory contains a little front-end to get the Google token and test it in our backend.

Build the node modules is important to execute it without problems, in case you want test locally.

```
npm install
```